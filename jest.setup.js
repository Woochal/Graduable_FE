// Increase timeout for Selenium tests
jest.setTimeout(30000);

// Global setup for Selenium WebDriver
beforeAll(async () => {
    // Add any global setup here
});

// Global teardown
afterAll(async () => {
    // Add any global cleanup here
});
